1. 保持大图容器的圆角 `rounded-t-3xl` 不变
2. 将顶部渐变的圆角从 `rounded-t-3xl` 调整为 `rounded-t-4xl`
3. 将左右渐变容器的圆角从 `rounded-t-3xl` 调整为 `rounded-t-4xl`
4. 将左侧渐变的圆角从 `rounded-tl-3xl` 调整为 `rounded-tl-4xl`
5. 将右侧渐变的圆角从 `rounded-tr-3xl` 调整为 `rounded-tr-4xl`
6. 确保所有渐变元素的圆角大于大图圆角，实现渐变联动效果

