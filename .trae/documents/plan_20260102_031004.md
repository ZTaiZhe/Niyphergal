1. 增大圆角：将当前的`rounded-t-2xl`调整为`rounded-t-3xl`，使圆角效果更明显
2. 减小上方渐变范围：将当前的`h-[10vh]`调整为`h-[5vh]`，减小上方渐变的垂直范围
3. 增大左右渐变宽度：将当前的`w-5`调整为`w-10`，增加左右渐变的水平宽度
4. 增强渐变效果：通过调整渐变颜色的透明度或增加渐变长度来增强效果
5. 保持其他样式不变，确保整体视觉效果协调

