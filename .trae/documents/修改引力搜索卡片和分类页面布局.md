## 实施计划

### 任务1：修改引力搜索卡片样式
**目标**：使引力搜索卡片与注册登录卡片保持一致

**修改文件**：`src/css/styles.css`
- 将 `wide-card` 类的 `max-width` 从 `600px` 改为 `400px`
- 保持其他样式不变（宽度、内边距、最小高度等）

### 任务2：修改分类页面卡片列数
**目标**：采用与首页游戏卡片一致的响应式动态列数算法

**修改文件**：`src/css/styles.css` 和 `src/js/pages/category.js`

1. **CSS修改**：
   - 创建新的 `.category-cards-container` 类
   - 复用首页游戏卡片的响应式列数逻辑：
     - 手机端：1列
     - 手机横屏：2列
     - 平板端：2列
     - 平板横屏：3列
     - 桌面端：3列
     - 大屏幕桌面端：4列

2. **JS修改**：
   - 将分类页面的 `grid-cols-2` 改为 `category-cards-container`
   - 调整卡片样式以适应新的布局