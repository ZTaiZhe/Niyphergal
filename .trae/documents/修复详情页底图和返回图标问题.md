1. **修复底图白边问题**：

   * 调整 `.top-image-container` 内图片的缩放比例，从 `scale-110` 改为 `scale-125`，确保图片填满容器

   * 确保容器没有内边距或外边距导致白边

2. **修复返回图标和文字位置问题**：

   * 调整返回按钮和文字的垂直位置，从 `top-10` 改为 `top-20`，避免与顶部固定的网站标题重叠

   * 确保返回图标和文字在视觉上清晰可见

3. **检查返回逻辑**：

   * 确认 `router.push('home')` 函数是否正确实现

   * 检查路由逻辑是否有问题，确保返回按钮能正常工作

4. **修改文件**：

   * 只修改 `index.html` 文件中的详情页相关代码

   * 具体修改位置：

     * 底图容器：line 2333-2342

     * 返回按钮和文字：line 2336-2341

5. **测试验证**：

   * 确保底图填满容器，没有白边

   * 确保返回图标和文字位置合适，不被网站标题遮挡

   * 确保返回按钮能正常返回到首页

