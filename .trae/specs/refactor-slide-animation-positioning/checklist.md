- [x] CSS 中 section 动画规则不再包含 position/top/left/width，仅保留 z-index
- [x] `animateSlideTransition()` 函数存在且签名正确
- [x] 旧 section 变为 absolute 前捕获 offsetTop/offsetLeft/offsetWidth
- [x] 旧 section 的内联定位样式精确匹配其正常流位置（不跳动）
- [x] 动画结束后移除旧 section 的内联 position/top/left/width 样式
- [x] 动画结束后恢复容器的 overflow 和 position
- [x] push 方向正确：旧页面左滑出、新页面右滑入
- [x] pop 方向正确：旧页面右滑出、新页面左滑入
- [x] preserveExisting 时 section DOM 状态保持
- [x] prefers-reduced-motion 时跳过动画直接切换
- [x] vite build 无错误
