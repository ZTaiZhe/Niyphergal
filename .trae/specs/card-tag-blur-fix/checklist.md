# 游戏卡片标签区域渐进模糊效果重构 - 检查清单

## 🧪 功能与边界测试

- [x] **全卡覆盖验证**：确认 `<img>` 的尺寸与整张 `.glass-card` 完全一致
- [x] **极限亮色图验证**：强制替换一张纯白色的封面图，确认卡片标题文字依然能够阅读
- [x] **零杂色污染验证**：在深色系和鲜艳系的图片上，确认标签区域的模糊完全透出底图原本的色彩
- [x] **iOS Safari 兼容验证**：确认底部两个圆角被完美裁切，没有模糊的直角方块溢出
- [x] **动画交互验证**：鼠标 Hover 触发卡片图片放大时，底部的渐进式模糊没有闪烁或定位偏移

## 🧪 生产级边缘测试

- [x] **长文本溢出测试**：将游戏标题修改为极长的三行文本，确认文本会在第二行末尾正确显示 `...`
- [x] **死链图片兜底测试**：故意传入一个无效的图片 URL，确认卡片能正常显示备用的 Fallback 图片
- [x] **响应式尺寸验证**：缩放浏览器窗口，确认卡片通过 `aspect-ratio` 能自适应维持优雅的比例
- [x] **键盘访问测试**：通过键盘按 Tab 键，确认能够选中卡片，并且按下 Enter 键能够触发跳转
- [x] **性能监控**：在 Chrome DevTools 开启 Frame Rendering Stats，高频 Hover 卡片，确认不会产生严重的重绘闪烁
