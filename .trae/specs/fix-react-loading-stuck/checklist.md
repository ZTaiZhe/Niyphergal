- [x] `flutter build web` 构建成功，无错误和警告
- [x] `build/web/flutter_service_worker.js` 文件存在
- [x] `build/web/main.dart.js` 文件存在且大小正常
- [x] `build/web/canvaskit/` 目录包含完整 CanvasKit 文件
- [x] `web/index.html` 包含加载超时 JS（15s 超时 → 显示错误提示）
- [x] `lib/main.dart` 设置了 `ErrorWidget.builder` 自定义错误页面
- [x] `build/web/_headers` CSP 包含 `script-src 'wasm-unsafe-eval'` 和 `worker-src 'self' blob:`
- [x] `build/web/_headers` CSP 包含 `style-src-attr 'unsafe-inline'` 和 `connect-src ... https://www.gstatic.com ...`
- [x] 部署成功：https://d3c39c93.niyphergal.pages.dev
- [ ] Chrome DevTools Console 无 CSP 违规报告
- [ ] 页面首次加载 3 秒内显示实际内容（不卡在 spinner）
- [ ] 模拟后端不可用时页面仍能正常显示本地数据
