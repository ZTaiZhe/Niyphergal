# Tasks

- [x] Task 1: 修改轮播图容器样式，用 `aspect-ratio` 替代固定高度
  - [x] 1.1 将 `.carousel-container` 的 `height: 240px` 替换为 `aspect-ratio: 16 / 9`
  - [x] 1.2 删除 `@media (min-width: 768px)` 中的 `.carousel-container { height: 420px }` 覆盖规则

# Task Dependencies
- 无依赖
