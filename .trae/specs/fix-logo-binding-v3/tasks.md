# 修复 Logo 按钮绑定失效问题（完整分析） - 任务列表

## 任务列表

- [x] Task 1: 将全局变量注册移到模块顶层
  - 在模块加载时立即注册 `window.LogoMenu` 等全局变量
  - ES 模块同步执行顶层代码，确保在任何用户交互前变量已注册
  - 从 `initApp()` 中移除全局变量注册

- [x] Task 2: 验证修复效果
  - 全局变量在模块加载时立即注册
  - Logo 按钮点击事件将立即生效

## 任务依赖
- [Task 2] depends on [Task 1]
