# 搜索栏 input 和按钮高度与 box 一致 - 任务列表

## 任务列表

- [x] Task 1: 调整 input wrapper 高度
  - 将 `.search-input-wrapper` 的 `height` 从 `32px` 改为 `40px`

- [x] Task 2: 调整搜索按钮尺寸
  - 将 `#header-search-btn` 的 `width` 和 `height` 从 `32px` 改为 `40px`

- [x] Task 3: 验证效果
  - 验证 input 高度与 box 一致
  - 验证按钮高度与 box 一致

## 任务依赖
- [Task 3] depends on [Task 1, Task 2]
