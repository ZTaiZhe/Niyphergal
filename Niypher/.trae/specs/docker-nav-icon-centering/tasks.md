# Docker 栏图标垂直居中修正 - 任务列表

## 任务列表

- [x] Task 1: 添加 CSS padding 样式
  - 为 `.nav-item` 添加 `padding: 6px 0`

- [x] Task 2: 移除 HTML h-full 类
  - 移除四个 `.nav-item` 按钮的 `h-full` 类
  - 解决 CSS padding 与 h-full 的冲突

- [x] Task 3: 验证效果
  - 验证 Docker 栏图标垂直居中
  - 验证图标不会超出卡片边界

## 任务依赖
- [Task 3] depends on [Task 2]
