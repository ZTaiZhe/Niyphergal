# Tasks

- [x] Task 1: 修复 performHeroNavigate 事件监听器时序
  - [x] SubTask 1.1: 将 animationHelpers.js 中 `container.addEventListener('detail:rendered', onRendered, { once: true })` 代码块从函数末尾移至 `routerInstance.push('detail', { id: targetId })` 之前

# Task Dependencies
- 无依赖
