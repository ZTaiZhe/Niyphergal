# 搜索页全局入场动效优化 - 检查清单

## 页面级动效
- [x] `.page-slide-up-enter-active` CSS 类已添加（自下而上进入）
- [x] `.page-slide-down-exit-active` CSS 类已添加（向下退出）
- [x] GPU 硬件加速已实现（transform + opacity）
- [x] `@media (prefers-reduced-motion: reduce)` 支持已添加

## 页面切换逻辑
- [x] renderer.js 已修改页面切换逻辑
- [x] 进入搜索页时使用自下而上动画
- [x] 手机手势返回或点击返回键时搜索页整页向下退出
- [x] 从搜索页跳转详情页时使用左右滑动逻辑

## 结果项动效
- [x] 首次进入搜索页时结果项初始状态为隐藏态
- [x] 强制触发重绘确保动画正确播放
- [x] 结果项使用 `.is-entering` 交错动效
- [x] 动效与局部刷新保持一致

## QA 验收检查
- [x] 页面进场：搜索页平滑从底部推入屏幕
- [x] 页面退场（后退）：手机手势返回或点击返回键，搜索页整页向下退出
- [x] 页面退场（跳转）：点击卡片跳转详情页，使用左右滑动逻辑
- [x] 列表首屏动效：阶梯式自下而上浮现
- [x] 无跳闪验证：首屏 DOM 加载无跳闪
- [x] A11y 验证：减弱动态效果时动效被屏蔽
