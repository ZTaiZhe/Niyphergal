# Tasks

- [x] Task 1: home.js — `revealHomeCardsImmediately` 增加 `is-hidden` 移除
  - [x] SubTask 1.1: 在循环内 `classList.add('is-loaded')` 之前添加 `cards[i].classList.remove('is-hidden');`

# Task Dependencies
- 无依赖
