# 亚克力/毛玻璃视觉质感升级 - 检查清单

## 🧪 UI 与跨平台验证

- [x] 亮/暗模式切换：触发主题切换时，面板背景色、边框高光和阴影透明度能平滑且正确地切换
- [x] 渐变与边框质感：检查卡片左上角是否有偏亮的白光（通过 135deg 渐变实现），并且存在半透明白色内边距描边（Border）
- [x] 底层穿透测试：在有复杂背景图或色彩的页面上滚动时，面板背后的图像能被正确模糊（12px），且色彩透过率适中
- [x] Safari 兼容性：使用 iOS Safari 或 Mac Safari 打开网页，确认 `backdrop-filter` 正常工作，未降级为纯色，且页面无闪烁 (Flicker) Bug
- [x] 滚动性能：在移动端快速滚动列表时，毛玻璃渲染没有导致明显的掉帧（FPS 稳定）
- [x] 一致性排查：NavBar、BottomNav、GameCard、SearchModal 全部统一使用了 `.acrylic-panel` 的 CSS 变量逻辑，没有遗漏的硬编码旧样式
